# 贡献指南

本仓库由 `@Misakago` 负责维护，社区贡献通过公开 Issue 和 Pull Request 进入。
请先用 Bug/Feature Issue Form 描述目标，再提交小而可验证的 PR。

- PR 标题使用 `type(scope?): 中文摘要`，例如 `fix(editor): 修复 Blob URL 清理`。
- 运行与改动相关的 lint、test、build，并在 PR 中记录结果。
- 不要在仓库或 Issue 中提交凭据、用户文档内容或未脱敏录屏。
- 行为、版本和发布策略以仓库文档与维护者评审为准。

安全问题请使用 [Private Vulnerability Reporting](https://github.com/agentbridges-ai/onlyoffice-browser/security/advisories/new)。
